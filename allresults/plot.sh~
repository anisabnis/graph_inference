#!/bin/bash 

networks=(6et1 tata sinet columbus geant5 bandcon bics integra rnp sanet dfn surfnet evolink)

for n in ${networks[@]}; do
    cp results_trees/${n}/mapping.txt allresults/mapping.${n}
done
