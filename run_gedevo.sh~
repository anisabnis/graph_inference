#!/bin/bash

#networks=(6et1 tata sinet columbus geant5 bandcon bics integra rnp sanet dfn surfnet att colt evolink)
networks=(6et1)
for n in ${networks[@]}; do
    python gen_gedevo_files.py experiment2/${n}
done